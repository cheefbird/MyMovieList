# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'MyMovieList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Private Keys
  plugin 'cocoapods-keys', {
    :project => 'MyMovieList',
    :keys => [
      "TMDbAPIKey"
    ]
  }

  # Pods for MyMovieList
  pod 'Alamofire', '~> 4.9.0'

end
